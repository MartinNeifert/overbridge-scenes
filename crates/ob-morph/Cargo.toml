[package]
name = "ob-morph"
version = "0.1.0"
edition = "2021"
description = "Crossfader morph math for Overbridge Scenes (shared by host API and remote VST)"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
